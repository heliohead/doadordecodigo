<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only"><%= t 'pages.layout.toggle_navigation' %></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t('pages.layout.title'), root_path, class: 'navbar-brand' %>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to t('pages.layout.menu.home'), root_path %></li>
      </ul>
      <ul class="nav navbar-nav pull-right">
        <li>
          <% if current_user %>
              <%= current_user.email %>,
              <%= link_to t('pages.layout.logout'), destroy_user_session_path, method: :delete %>
          <% else %>
              <%= link_to t('pages.layout.login'), new_user_session_path %>
          <% end %>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>